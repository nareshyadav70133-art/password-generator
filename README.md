# password-generator
Password Generator is a Python-based application that creates strong and secure passwords randomly. It allows users to generate passwords using a combination of uppercase letters, lowercase letters, numbers, and special characters. The project demonstrates the use of Python random module, string handling, and user input.
